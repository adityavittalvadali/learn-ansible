- name: variables Demo
  hosts: localhost
  vars:
    URL: https://www.google.com

  tasks:
    - name: Print URL
      debug:
        msg: URL - "{{ URL }}"

    - name: Print fruit name
      debug:
        msg: fruit - "{{ fruit }}"
        vars:
          fruit: banana